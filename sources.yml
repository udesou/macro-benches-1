# Pinned source versions for vendored packages.
#
# This file is the single source of truth: the vendor scripts and
# setup-monorepo.sh read it through scripts/lib-sources.sh (`src_field`,
# `clone_pinned`) rather than carrying their own copies of a version. Change a
# version or a commit here, re-run `make setup`, and that is the whole bump.
#
# Everything here is pinned by construction — tarballs by version + md5, git
# sources by commit. Nothing tracks a branch HEAD. Six git sources used to
# (ppxlib, lwt, merlin, js_of_ocaml, pplacer, mcl), which meant a cold setup
# vendored whatever upstream had that morning; by the time they were pinned two
# had already drifted away from the validated tree and one branch had been
# deleted upstream, breaking a cold setup outright.

# --- menhir (parser generator) ---
menhir:
  version: "20260209"
  url: "https://gitlab.inria.fr/fpottier/menhir/-/archive/20260209/archive.tar.gz"
  md5: "e993231085db95ab011ffe0cd606d9dd"

# --- cpdf + camlpdf (PDF command-line tool) ---
camlpdf:
  version: "2.8.1"
  url: "https://github.com/johnwhitington/camlpdf/archive/refs/tags/v2.8.1.tar.gz"
  md5: "93883956f317d4037341b6a0da8a6d03"
  notes: "OCaml PDF library — dependency of cpdf.  Uses OCamlMakefile; dune overlay in dune-overlays/camlpdf/."

cpdf-source:
  version: "2.8.1"
  url: "https://github.com/johnwhitington/cpdf-source/archive/refs/tags/v2.8.1.tar.gz"
  md5: "45ba51aae6b5d3ea6cb421037f8f73bb"
  notes: "PDF command-line tool.  Uses OCamlMakefile; dune overlay in dune-overlays/cpdf-source/."

# --- rocq / coq (proof assistant) ---
zarith:
  version: "1.14"
  url: "https://github.com/ocaml/Zarith/archive/refs/tags/release-1.14.tar.gz"
  md5: "05ef6f78ebea92aa5c3ff35bbfd29bf9"
  notes: "GMP wrapper.  Uses configure/make; dune overlay in dune-overlays/zarith/.  System dep: libgmp-dev."

rocq:
  version: "9.0.0"
  url: "https://github.com/rocq-prover/rocq/archive/refs/tags/V9.0.0.tar.gz"
  md5: "e6c2f84353384300a47b2e90d6824229"
  notes: "Proof assistant (formerly Coq).  Uses dune natively.  Needs dunestrap + findlib library."

# --- alt-ergo (SMT solver) ---
alt-ergo:
  version: "2.6.2"
  url: "https://github.com/OCamlPro/alt-ergo/archive/refs/tags/v2.6.2.tar.gz"
  md5: "e4b1ffc9541d75bee8e02a5e40b68cc0"
  notes: "SMT solver.  Uses dune.  Many external deps — see vendor-alt-ergo.sh."

dolmen:
  version: "0.10"
  url: "https://github.com/Gbury/dolmen/archive/refs/tags/v0.10.tar.gz"
  md5: "a89db80202d2f2d0cd7ea73c46d5e5e1"
  notes: "SMT/TPTP parser.  Uses dune.  Alt-ergo dep."

ocplib-simplex:
  version: "0.5.1"
  url: "https://github.com/OCamlPro/ocplib-simplex/archive/refs/tags/v0.5.1.tar.gz"
  md5: "b6cb1e4852046a6aecfcfbbb94ecee51"
  notes: "Simplex solver.  Uses dune.  Alt-ergo dep."

psmt2-frontend:
  version: "0.4.0"
  url: "https://github.com/ACoquereau/psmt2-frontend/archive/refs/tags/0.4.0.tar.gz"
  md5: "4fc9f9a68343b4614f120c1556bee644"
  notes: "PSMT2 parser.  Uses dune.  Alt-ergo dep."

cmdliner:
  version: "1.3.0"
  url: "https://github.com/dbuenzli/cmdliner/archive/refs/tags/v1.3.0.tar.gz"
  md5: "49209ae417b18910eca24bd2ab6538da"
  notes: "CLI argument parser.  Uses dune.  Alt-ergo dep."

ppx_blob:
  version: "0.9.0"
  url: "https://github.com/johnwhitington/ppx_blob/archive/refs/tags/0.9.0.tar.gz"
  md5: "8dc76254a66ed47aaab6e612ce5e913c"
  notes: "PPX for embedding files as strings.  Uses dune.  Alt-ergo dep."

# =============================================================================
# Git sources, pinned by commit
# =============================================================================
# Cloned by clone_pinned() (scripts/lib-sources.sh), which fetches the commit
# directly and keeps .git so `git -C <dir> rev-parse HEAD` tells you what you
# have. `branch` is where the commit came from — provenance only; it is not used
# to fetch, so a branch that moves or disappears upstream cannot change what a
# cold setup vendors.

# --- replacements for lockfile packages (see CLAUDE.md patches 4 + 5) ---
ppxlib:
  repo: "https://github.com/ocaml-ppx/ppxlib.git"
  branch: "main"
  commit: "df9d10b5163b35d646b75dc412237280e43273d0"
  date: "2026-06-01"
  notes: "Replaces the lockfile's ppxlib; adds Ast_506 for OCaml 5.6 trunk. Drop once a release supports 5.6."

lwt:
  repo: "https://github.com/ocsigen/lwt.git"
  branch: "master"
  commit: "167a6ea45363647be9cc58dde9155d9e2f176e69"
  date: "2026-04-20"
  notes: "Replaces the lockfile's lwt; fixes socketaddr.h for OCaml 5.6. Drop once a release supports 5.6."

# --- vendored tools not in the lockfile ---
js_of_ocaml:
  repo: "https://github.com/ocsigen/js_of_ocaml.git"
  branch: "master"
  commit: "8b46f0628091b73513141a010c3ac3914a872395"
  date: "2026-07-28"
  notes: >
    Released jsoo rejects OCaml >= 5.5 outright. Support landed on master (the
    `[ 5; 7 ]` bound in compiler/lib/magic_number.ml), so master is the pin. This
    used to track the `ocaml-5.6` PR branch, which upstream squashed and deleted —
    the old commit is no longer reachable, so a cold clone of that branch fails.
    Note ocamlc-self-compile also takes its workload from this tree
    (benchmarks/sources/ml), so bumping this changes two benchmarks.

merlin:
  repo: "https://github.com/ocaml/merlin.git"
  branch: "merlin-domains"
  commit: "7f033f9352531e0179f5d3a1f0000bb1be740844"
  date: "2025-10-03"
  notes: "PR #1890 domains branch; benchmark is DISABLED (upstream race, see benchmarks/merlin/UPSTREAM_BUG.md). Vendored but not built."

pplacer:
  repo: "https://github.com/matsen/pplacer.git"
  branch: "master"
  commit: "40509ec8017d6d899bdbc3659260ae32e5be5953"
  date: "2026-01-28"
  notes: "Not in opam. Needs libgsl-dev + libsqlite3-dev. Patched by setup-monorepo.sh (patch 13)."

mcl:
  repo: "https://github.com/fhcrc/mcl.git"
  branch: "master"
  commit: "1f1932b64619e9bd9ecbcb421cb1e3f1eb535e80"
  date: "2025-12-01"
  notes: "pplacer's git submodule (SSH URL upstream, so cloned over HTTPS separately). Built in place by configure/make. Patched by setup-monorepo.sh (patch 12)."

# --- apron chain for goblint (non-dune: configure/make + camlidl) ---
# Built per-runtime into a self-contained prefix by scripts/vendor-apron.sh.
# The `branch` field here names the release tag the commit was resolved from.
# Pinning the commit rather than the tag means a re-pointed tag upstream cannot
# change what we build.
bigarray-compat:
  repo: "https://github.com/mirage/bigarray-compat.git"
  branch: "v1.1.0"
  commit: "2ea842ba4ab2cfee7b711f7ad927917f3179a6f9"

camlidl:
  repo: "https://github.com/xavierleroy/camlidl.git"
  branch: "camlidl113"
  commit: "9f474e484982dacb0cf6284059b004a9bf6639b6"

mlgmpidl:
  repo: "https://github.com/nberth/mlgmpidl.git"
  branch: "1.3.0"
  commit: "00937842dca353faf81dbee4cf5ca00689de9faa"

apron:
  repo: "https://github.com/antoinemine/apron.git"
  branch: "v0.9.15"
  commit: "b3172bd51e4bb0135716a300cab424a56970d96f"

# =============================================================================
# Other pinned downloads
# =============================================================================
frama-c:
  version: "32.1"
  repo: "https://git.frama-c.com/pub/frama-c.git"
  branch: "32.1"
  commit: "3fd8ce95f47f39ba632732669bff6e9ab7aa4b78"
  notes: "32.1 is not in opam, so it is cloned from git; only a trimmed kernel+EVA is built. Needs libyaml + pkg-config. `branch` is the release tag the commit came from."

libevent:
  version: "0.9.0"
  url: "https://ygrek.org/p/release/ocaml-libevent/ocaml-libevent-0.9.0.tar.gz"
  md5: "00851213da0c9ecf37fbd3984a266789"
  notes: "devkit dep. Hand-written dune overlay; patched for OCaml 5.x labels (patch 7)."

ocurl:
  version: "0.9.2"
  url: "https://ygrek.org/p/release/ocurl/ocurl-0.9.2.tar.gz"
  md5: "8a4149c6cfa79dabdfde24f308ad1f0c"
  notes: "devkit dep. System dep: libcurl4-openssl-dev."

cmdliner-dune:
  version: "2.1.1+dune"
  url: "https://github.com/dune-universe/cmdliner/releases/download/v2.1.1%2Bdune/cmdliner-2.1.1.dune.tbz"
  notes: >
    Distinct from the `cmdliner` entry above (1.3.0, the alt-ergo vendor path).
    js_of_ocaml needs the 2.x Arg.Completion API, and upstream dbuenzli/cmdliner
    ships no dune files, so this is the dune-universe overlay build. Replaces the
    lockfile's cmdliner in duniverse/.

# --- alt-ergo deps not yet vendored ---
# ppx_deriving v6.0.3 — https://github.com/ocaml-ppx/ppx_deriving — needs ppxlib ecosystem
# camlzip      rel113  — https://github.com/xavierleroy/camlzip     — Makefile, needs dune overlay + zlib
# fmt          v0.9.0  — https://github.com/dbuenzli/fmt            — topkg, needs dune overlay
# logs         v0.7.0  — https://github.com/dbuenzli/logs           — topkg, needs dune overlay

# --- infer benchmark: manually vendored, java-only pure-dune build ---
# Infer itself (autoconf/make upstream generates dune files, so it can't join the
# opam-monorepo lock — vendored via scripts/vendor-infer.sh with a pre-generated
# java-only dune overlay) plus its non-dune analysis-lib chain (cppo, extlib,
# camlzip, javalib, sawja) built per-runtime by scripts/vendor-javalib-sawja.sh.
# Commits are the resolved targets of the tags these scripts previously cloned by
# name.
cppo:
  repo: "https://github.com/ocaml-community/cppo.git"
  branch: "v1.8.0"
  commit: "a3e90caf8fe416000f8aff8e1b1e7ee3b309d589"
  notes: "Build-time preprocessor for extlib. Built into the per-runtime prefix by scripts/vendor-javalib-sawja.sh so the chain needs no `opam install cppo`."

processor:
  version: "0.2"
  url: "https://github.com/haesbaert/ocaml-processor/archive/refs/tags/0.2.tar.gz"
  md5: "6ce02489ebfb40789fae78c2eb9a992e"
  notes: "CPU topology + per-thread affinity (pthread_setaffinity_np on Linux and FreeBSD, a no-op on macOS). Vendored into vendor/processor by setup-monorepo.sh. Used by infer's domain pinning, and by lavyek_bench.ml when that is re-enabled. Only Affinity.get_ids/set_ids are used -- they are direct C externals, so they do not go through Topology, which has a real implementation only on amd64."

infer:
  repo: "https://github.com/ngorogiannis/infer.git"
  branch: "inferbench-v1.1"
  commit: "49f84c822647a1d35c9961e0e13cc5186277f592"
  date: "2026-08-19"
  notes: "java-only Infer fork; tag inferbench-v1.1. Vendored by scripts/vendor-infer.sh, built as an in-tree dune project via the dune-overlays/infer overlay."

extlib:
  repo: "https://github.com/ygrek/ocaml-extlib.git"
  branch: "1.8.0"
  commit: "037e18fd59575fd7cebae5887348c7ca320d91fe"
  notes: "javalib/sawja dep (infer). Built per-runtime, opam-free, by scripts/vendor-javalib-sawja.sh."

camlzip:
  repo: "https://github.com/xavierleroy/camlzip.git"
  branch: "rel111"
  commit: "d81554a31865064b2c5719a53265fd52a6f3d380"
  notes: "javalib/sawja dep (infer), zip 1.11 to match the lock. Built per-runtime by scripts/vendor-javalib-sawja.sh."

javalib:
  repo: "https://github.com/javalib-team/javalib.git"
  branch: "3.2.2"
  commit: "8d9829c5c2061b60c31874e89d9d9bd44118d2de"
  notes: "infer's bytecode-capture library. Built per-runtime by scripts/vendor-javalib-sawja.sh."

sawja:
  repo: "https://github.com/javalib-team/sawja.git"
  branch: "1.5.12"
  commit: "9b6da230669b11c009d97ed3851513a1d39af268"
  notes: "infer's static-analysis library (on javalib). Built per-runtime by scripts/vendor-javalib-sawja.sh."
